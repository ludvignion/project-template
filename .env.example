# Every secret the project reads, by name, with no value. Copy to .env and fill in.
# The verdict sets each of these to a dummy value and runs the tests with sockets blocked:
# a test that reaches the network with a key present is a block.
